# Core dependencies for ICR
torch>=2.0.0
transformers>=4.30.0
datasets>=2.0.0
pyserini>=0.20.0
pandas>=1.5.0
numpy>=1.20.0
tqdm>=4.60.0
beir>=2.0.0
accelerate>=0.20.0
huggingface-hub>=0.15.0

# Optional dependencies
# For flash attention (if use_fa2=True):
# flash-attn>=2.0.0  # Install separately: pip install flash-attn --no-build-isolation
